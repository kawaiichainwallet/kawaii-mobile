import 'package:flutter/material.dart';

/// 创建钱包流程页面
class CreateWalletFlowScreen extends StatelessWidget {
  const CreateWalletFlowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('创建钱包'),
      ),
      body: const Center(
        child: Text('创建钱包流程 - 待实现'),
      ),
    );
  }
}
